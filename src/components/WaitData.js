import { CircularProgress } from '@material-ui/core';
import React from 'react';

const WaitData = () => {
  return (
    <section className='waitData'>
      <CircularProgress color="secondary"/>
    </section>
  );
};
export default WaitData;
